[advisories]
# atomic-polyfill is transitive via postcard -> heapless.
# It's flagged unmaintained but has no known vulnerabilities.
# Remove this once postcard/heapless drop the dependency.
ignore = ["RUSTSEC-2023-0089"]
